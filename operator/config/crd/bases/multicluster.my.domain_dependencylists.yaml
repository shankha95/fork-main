---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: dependencylists.multicluster.my.domain
spec:
  group: multicluster.my.domain
  names:
    kind: DependencyList
    listKind: DependencyListList
    plural: dependencylists
    singular: dependencylist
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: DependencyList is the Schema for the dependencylists API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DependencyListSpec defines the desired state of DependencyList
            properties:
              clusterName:
                type: string
              clusterServiceMap:
                additionalProperties:
                  items:
                    type: string
                  type: array
                type: object
              dependencies:
                description: Dependencies is a list of dependencies
                items:
                  properties:
                    dependent:
                      type: string
                    dependsOn:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: DependencyListStatus defines the observed state of DependencyList
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
